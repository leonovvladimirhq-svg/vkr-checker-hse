# VKR Checker

Веб-сервис автоматической проверки магистерских ВКР и курсовых работ для образовательной программы **«Интегрированные коммуникации»** Школы коммуникаций НИУ ВШЭ.

Студент загружает документ (`.docx` / `.pdf`), указывает ссылку на базу данных (Яндекс.Диск) — сервис анализирует работу через OpenAI GPT по чек-листу методических материалов программы и выдаёт подробный отчёт.

**Продакшен:** [вкр-чекер.рф](https://xn----ctbkawc3be3d.xn--p1ai) (Punycode: `xn----ctbkawc3be3d.xn--p1ai`, IP `89.125.107.91`).

**Полная техническая документация** — в [CLAUDE.md](./CLAUDE.md) (архитектурные решения, схема БД, история изменений). Этот README — точка входа для нового разработчика.

---

## Содержание

1. [Что умеет сервис](#что-умеет-сервис)
2. [Стек](#стек)
3. [Быстрый старт (локальная разработка)](#быстрый-старт-локальная-разработка)
4. [Структура репозитория](#структура-репозитория)
5. [Ключевые модули](#ключевые-модули)
6. [Учётные данные](#учётные-данные)
7. [База данных](#база-данных)
8. [Деплой на продакшен](#деплой-на-продакшен)
9. [Сопровождение](#сопровождение)
10. [Контакты](#контакты)

---

## Что умеет сервис

В сервисе **два независимых контура**, объединённых одной авторизацией и общей панелью преподавателя:

### 1. Модуль ВКР (`/`, `/teacher`, `/report`)
Проверка магистерских диссертаций (ИКР) и магистерских проектов (КП) по чек-листу. Выдаёт **три статуса**:
- **Зачёт** (`pass`) — все автоматические пункты пройдены, ручных нет;
- **Незачёт** (`fail`) — хотя бы один автоматический пункт провален;
- **Ожидает проверки** (`pending`) — есть пункты, которые нельзя проверить программно (длительность аудио, качество звука и т. п.) — финальное решение за преподавателем.

Преподаватель видит сводную таблицу всех студентов с попытками, статусами, может скачать загруженные работы, проставить итоговый зачёт/незачёт, опубликовать итоговый отчёт (со снапшотом результатов).

### 2. Модуль курсовых работ (`/course`)
Принципиально другой подход — **не pass/fail, а рекомендации**. ИИ-консультант анализирует работу, сверяет с методичками ОП ИК (2024–2025) и Программой практики 2025, выдаёт:
- общую оценку готовности (5 уровней — от «критические замечания» до «близок к зачёту»);
- сильные стороны работы;
- что стоит доработать;
- поразделный разбор структуры;
- **анализ базы данных** с Яндекс.Диска (доступность, состав файлов по типам, соответствие методам исследования);
- приоритетные рекомендации.

При отправке преподавателю генерируется Word-отзыв по шаблонам Приложения 20.1 / 20.2 Программы практики (с автозаполнением полей через GPT, красной пометкой о ручной проверке там, где авто-проверка невозможна, и обязательной декларацией использования ИИ в конце).

---

## Стек

| Слой | Технология |
|------|-----------|
| Фронтенд + бэкенд | **Next.js 14** (App Router), React, TypeScript |
| Стили | Tailwind CSS |
| База данных | SQLite через `better-sqlite3`, файл `data/vkr.db` |
| AI | OpenAI API (модель в `.env`, по умолчанию `gpt-5.2`) |
| Парсинг документов | `mammoth` (.docx), `pdf-parse` (.pdf), `xlsx` (Excel) |
| Word-генератор | `docx` (для отзывов на курсовые) |
| Интеграция | Яндекс.Диск API (публичные ссылки, без OAuth) |
| Email | `nodemailer` (опционально — для email-дайджестов преподавателю) |
| Процесс-менеджер | PM2 (production) |
| Веб-сервер | Nginx (reverse proxy + SSL через Certbot/Let's Encrypt) |

---

## Быстрый старт (локальная разработка)

**Требования:** Node.js 20+, npm 10+, Git.

```bash
# 1. Клонировать репозиторий
git clone https://github.com/leonovvladimirhq-svg/vkr-checker-hse.git
cd vkr-checker-hse
git checkout vkr-version-1

# 2. Установить зависимости
npm install

# 3. Создать файл .env в корне проекта
# Минимально нужен только OPENAI_API_KEY:
cat > .env <<EOF
OPENAI_API_KEY=sk-...
OPENAI_MODEL=gpt-5.2
EOF

# 4. Запустить в режиме разработки
npm run dev
# Сервис доступен на http://localhost:3000
```

Папка `data/` и файл `data/vkr.db` создаются автоматически при первом запуске.

### Доступные npm-скрипты

| Команда | Что делает |
|---------|-----------|
| `npm run dev` | Запуск dev-сервера на `localhost:3000` с hot-reload |
| `npm run build` | Production-сборка (`.next/`) |
| `npm start` | Запуск production-сборки |
| `npm run lint` | Проверка ESLint |

### Переменные окружения (`.env`)

| Переменная | Обязательная | Описание |
|------------|--------------|----------|
| `OPENAI_API_KEY` | **Да** | Ключ OpenAI API |
| `OPENAI_MODEL` | Нет | Модель GPT. По умолчанию `gpt-5.2`. Поддерживаются `gpt-4o-mini`, `gpt-5`, `gpt-5-nano`, `o3-mini` и др. — параметры (temperature / max_tokens vs max_completion_tokens) подбираются автоматически |
| `SMTP_HOST`, `SMTP_PORT`, `SMTP_USER`, `SMTP_PASS` | Нет | Для email-дайджеста преподавателю (опционально) |
| `DIGEST_RECIPIENT` | Нет | Email получателя дайджеста |

---

## Структура репозитория

```
vkr-checker-app/
├── src/
│   ├── app/                        # Next.js App Router
│   │   ├── page.tsx                # Главная страница студента (ВКР)
│   │   ├── teacher/page.tsx        # Панель преподавателя (общая для ВКР и курсовых)
│   │   ├── report/page.tsx         # Итоговый отчёт / статус работы для студентов
│   │   ├── course/page.tsx         # Самопроверка курсовых
│   │   └── api/
│   │       ├── check/route.ts      # POST: анализ ВКР
│   │       ├── attempts/route.ts   # CRUD попыток ВКР
│   │       ├── report/route.ts     # Итоговый отчёт ВКР (флаг + снапшот)
│   │       ├── download/route.ts   # Скачивание загруженных работ ВКР
│   │       ├── students/route.ts   # Настройки и список студентов
│   │       ├── digest/route.ts     # Email-дайджест преподавателю
│   │       └── course/             # Все эндпоинты курсовых
│   │           ├── check/route.ts      # POST: анализ курсовой + проверка БД
│   │           ├── submit/route.ts     # POST: отправка работы преподавателю
│   │           ├── teacher/route.ts    # GET: список + детали для панели
│   │           ├── download/route.ts   # GET: оригинал работы
│   │           └── reanalyze/route.ts  # POST: повторный анализ + генерация Word-отзыва
│   ├── lib/
│   │   ├── analyzer.ts             # Промпт и вызов GPT для ВКР
│   │   ├── checklist.ts            # Динамический чек-лист ВКР по методам
│   │   ├── parser.ts               # Парсинг .docx/.pdf + computeBodyVolume
│   │   ├── db.ts                   # SQLite: таблицы attempts, settings
│   │   ├── db-analyzer.ts          # Анализ файлов БД с Яндекс.Диска
│   │   ├── authCache.ts            # Кэш авторизации в localStorage (TTL 24ч)
│   │   ├── yandex-disk.ts          # Яндекс.Диск API (публичные ссылки, рекурсия до 5 уровней)
│   │   ├── course-analyzer.ts      # Промпт и вызов GPT для курсовых
│   │   ├── course-review-doc.ts    # Word-генератор отзыва (Приложение 20.1/20.2)
│   │   ├── course-review-prompt.ts # GPT-заполнение полей шаблона отзыва
│   │   ├── db-course.ts            # SQLite: таблица course_attempts
│   │   └── methodology/            # Методические материалы (.md) для контекста GPT
│   │       ├── index.ts
│   │       ├── coursework-2024.md  # Методичка 2024–2025 г. набора
│   │       ├── coursework.md       # LEGACY-методичка 22/23 г. набора
│   │       └── programme-2025.md   # Программа практики 2025
│   └── types.d.ts
├── data/                           # Создаётся автоматически
│   ├── vkr.db                      # SQLite база
│   ├── uploads/                    # Загруженные файлы ВКР
│   └── course-uploads/             # Загруженные файлы курсовых (только после Submit)
├── public/                         # Статика Next.js
├── ecosystem.config.js             # Конфиг PM2 (production)
├── next.config.js                  # Конфиг Next.js
├── tailwind.config.js              # Конфиг Tailwind
├── tsconfig.json                   # Конфиг TypeScript
├── CLAUDE.md                       # Подробная техническая документация (для AI/глубокого погружения)
└── README.md                       # Этот файл
```

---

## Ключевые модули

### Промпт-инжиниринг
- **ВКР:** `src/lib/analyzer.ts` — системный промпт + чек-лист передаются в GPT, ответ парсится как JSON. Поддерживает множество моделей с автоматическим выбором параметров.
- **Курсовые:** `src/lib/course-analyzer.ts` — роль «ИИ-консультант по курсовым работам». `EXPECTED_SECTIONS` содержат `synonyms` + `keywords` для семантического сопоставления заголовков (студентский «бриф заказчика» матчится с каноническим «ТЗ»).

### Подсчёт объёма работы
`src/lib/parser.ts` → `computeBodyVolume(text)` ищет границы тела работы:
- начало — заголовок «Введение» (или после титульника по эвристике);
- конец — «Список литературы / источников» или «Приложение».

Возвращает `bodyCharCountWithSpaces` (главная метрика — порог 90 000 для рус), `bodyCharCountNoSpaces`, `bodyWordCount`, `appendixWordCount` и флаги `volumeBreakdown` (что удалось распознать).

### Проверка базы данных (Яндекс.Диск)
`src/lib/yandex-disk.ts` — рекурсивный обход публичной папки до 5 уровней с пагинацией, таймаутами и лимитом 300 файлов. `src/lib/db-analyzer.ts` — скачивает таблицы и документы (до 10 МБ), парсит структуру (имена листов, колонки, превью), формирует текстовое описание для GPT.

Используется и в ВКР, и в курсовом контурах без модификаций.

### Word-генератор отзыва
`src/lib/course-review-doc.ts` — рендер `.docx` через библиотеку `docx`. Структура соответствует шаблонам Приложения 20.1 (ИКР) и 20.2 (КП). Особенности:
- Жирная красная пометка (`color: C00000`, bold, 11pt) **«Рекомендуется ручная проверка»** — там, где авто-проверка невозможна (БД при null-счётчиках, корректность цитирования → Антиплагиат, ИИ-критерии при `yesNo === '—'`).
- **AI-дисклеймер** курсивом 10pt после подписи руководителя — обязательная декларация использования ИИ-помощника (этическое требование, симметричное правилам декларирования ИИ для студентов).

---

## Учётные данные

Авторизация **клиентская** (без токенов, localStorage TTL 24 часа). Подходит для контролируемой учебной среды.

| Роль | Логин | Пароль |
|------|-------|--------|
| Студент (ВКР, курсовые, отчёт) | `student` | `hse2025` |
| Преподаватель | `admin 1029` | `hsevkrch12` |
| Пароль публикации итогового отчёта | — | `1234` |

Пароль публикации хранится в константе `REPORT_PASSWORD` в `src/app/api/report/route.ts`.

---

## База данных

SQLite, файл `data/vkr.db`. Миграции **ленивые** — выполняются при первом запросе через `initDb()` / `ensureSchema()` (для каждой таблицы).

### Таблицы

| Таблица | Назначение |
|---------|-----------|
| `attempts` | Попытки проверки ВКР |
| `course_attempts` | Попытки проверки курсовых |
| `settings` | Глобальные настройки (`digest_email`, `current_wave`, `report_generated_at`, `report_snapshot_ids`) |

**Полная схема и описание полей** — в [CLAUDE.md](./CLAUDE.md#схема-бд-srclibdbts).

### Бэкап базы
```bash
# На сервере:
cp /root/vkr-checker-hse/data/vkr.db /root/backups/vkr-$(date +%Y%m%d-%H%M).db
```

---

## Деплой на продакшен

### Сервер
- **OS:** Ubuntu 24.04, 1 CPU, 1 ГБ RAM + 1 ГБ Swap (`/swapfile`).
- **IP:** `89.125.107.91`.
- **Домен:** `вкр-чекер.рф` (Punycode `xn----ctbkawc3be3d.xn--p1ai`).
- **Путь проекта:** `/root/vkr-checker-hse/`.
- **Nginx:** reverse proxy на `localhost:3000`, SSL через Certbot/Let's Encrypt. Конфиг: `/etc/nginx/sites-available/vkr-checker`. Важные параметры: `client_max_body_size 50m` (загрузка файлов), `proxy_read_timeout 180` (долгие GPT-запросы).
- **PM2:** процесс `vkr-checker`, лимит памяти Node.js `--max-old-space-size=768` (см. `ecosystem.config.js`). Автозапуск через `pm2 startup`.

### Обновление сервера

После пуша новых коммитов в `vkr-version-1`:
```bash
ssh root@89.125.107.91
cd /root/vkr-checker-hse
git pull origin vkr-version-1
npm install
npm run build
pm2 restart vkr-checker
pm2 status  # проверка, что процесс online
```

### Откат
```bash
ssh root@89.125.107.91
cd /root/vkr-checker-hse
git log --oneline -10           # найти предыдущий коммит
git checkout <commit-hash>      # вернуться на него
npm install && npm run build
pm2 restart vkr-checker
```

---

## Сопровождение

### Логи
```bash
pm2 logs vkr-checker             # живой лог
pm2 logs vkr-checker --lines 200 # последние 200 строк
pm2 flush                        # очистить логи
```

### Перезагрузка процесса без потери коннектов
```bash
pm2 reload vkr-checker  # graceful reload
```

### Если процесс упал по OOM
- проверь `pm2 status` (поле «↺» — сколько раз перезапускался);
- проверь свободную память: `free -h`;
- свап включён? `swapon --show` (должен показать `/swapfile` 1G);
- лимит Node.js: `--max-old-space-size=768` в `ecosystem.config.js`.

### Обновление модели GPT
Достаточно изменить `OPENAI_MODEL` в `.env` и `pm2 restart vkr-checker`. Параметры (`temperature`, `max_tokens` vs `max_completion_tokens`, поддержка JSON-mode) подбираются автоматически в `getModelParams()` (см. `src/lib/analyzer.ts` и `src/lib/course-analyzer.ts`).

### Добавление новых методических материалов
Положи файл `.md` в `src/lib/methodology/` и подключи в `index.ts` через функцию `getMethodologyForCourse(type)` — содержимое автоматически попадёт в системный промпт GPT.

### Где менять что
| Хочу изменить | Где |
|----------------|-----|
| Чек-лист ВКР, пункты по методам | `src/lib/checklist.ts` |
| Промпт для ВКР | `src/lib/analyzer.ts` (функция `buildSystemPrompt`) |
| Промпт для курсовых | `src/lib/course-analyzer.ts` (функция `buildSystemPrompt`) |
| Ожидаемые разделы курсовой + синонимы | `EXPECTED_SECTIONS` в `src/lib/course-analyzer.ts` |
| Шаблон Word-отзыва на курсовую | `src/lib/course-review-doc.ts` |
| Промпт для заполнения Word-отзыва | `src/lib/course-review-prompt.ts` |
| Учётные данные | `src/app/page.tsx`, `src/app/teacher/page.tsx`, `src/app/course/page.tsx` (клиентская авторизация) |
| Пароль публикации отчёта | константа `REPORT_PASSWORD` в `src/app/api/report/route.ts` |

### Что НЕ покрыто автоматически (требует ручной проверки преподавателем)
- Длительность аудиозаписей и качество звука.
- Корректность цитирования и оригинальность (требует системы «Антиплагиат»).
- Содержание презентации (только сам факт ссылки проверяется).
- Любые разделы работы, вставленные изображениями (без распознаваемого текста).

Эти места явно помечены в UI (для студента) и **жирной красной пометкой** в Word-отзыве (для преподавателя).

---

## Контакты

- **Разработчик:** Владимир Леонов — [vleonov@hse.ru](mailto:vleonov@hse.ru)
- **GitHub:** [leonovvladimirhq-svg/vkr-checker-hse](https://github.com/leonovvladimirhq-svg/vkr-checker-hse)
- **Основная ветка разработки:** `vkr-version-1`

Для детального понимания архитектурных решений, истории изменений и описания всех полей БД — см. [CLAUDE.md](./CLAUDE.md).
