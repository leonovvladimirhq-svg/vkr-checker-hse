# VKR Checker — Сервис автоматической проверки магистерских работ

## Описание проекта
Веб-сервис для автоматической проверки ВКР (магистерских диссертаций и проектов) по чек-листу. Студент загружает документ (.docx/.pdf), указывает ссылку на базу данных (Яндекс.Диск) и презентацию — система анализирует работу через OpenAI GPT и выдаёт подробный отчёт.

Проект разработан для образовательной программы «Интегрированные коммуникации» НИУ ВШЭ.

## Технологический стек
- **Frontend + Backend:** Next.js 14 (App Router), React, TypeScript
- **Стили:** Tailwind CSS
- **БД:** SQLite (через better-sqlite3), файл `data/vkr.db`
- **AI:** OpenAI API (модель задаётся в `.env`, по умолчанию `gpt-4o-mini`)
- **Парсинг документов:** mammoth (docx), pdf-parse (pdf), xlsx (Excel)
- **Интеграция:** Яндекс.Диск API (публичные ссылки, без OAuth)
- **Деплой:** PM2 + Nginx (reverse proxy) + Certbot (SSL)
- **Домен:** вкр-чекер.рф (Punycode: `xn----ctbkawc3be3d.xn--p1ai`)

## Структура проекта
```
src/
├── app/
│   ├── page.tsx              # Главная страница (интерфейс студента)
│   ├── teacher/page.tsx      # Панель преподавателя (авторизация, сводная таблица)
│   ├── report/page.tsx       # Итоговый отчёт для студентов (поиск по ФИО)
│   └── api/
│       ├── check/route.ts    # POST: проверка ВКР (загрузка файла → GPT → результат)
│       ├── attempts/route.ts # GET/POST/PATCH/DELETE: управление попытками
│       ├── report/route.ts   # GET/POST: итоговый отчёт (флаг + live-данные)
│       ├── download/route.ts # GET: скачивание загруженных файлов работ
│       ├── students/route.ts # GET/POST: список студентов, настройки
│       └── digest/route.ts   # POST: email-дайджест для преподавателя
├── lib/
│   ├── analyzer.ts           # Промпт GPT, вызов OpenAI API, mergeResults()
│   ├── checklist.ts          # Определение чек-листа (пункты зависят от типа работы и методов)
│   ├── parser.ts             # Парсинг .docx (mammoth) и .pdf (pdf-parse)
│   ├── db.ts                 # SQLite: таблицы attempts, settings; CRUD-функции
│   ├── db-analyzer.ts        # Анализ файлов БД с Яндекс.Диска (xlsx/csv/docx/pdf)
│   ├── authCache.ts          # Кэширование авторизации в localStorage (TTL 24 часа)
│   └── yandex-disk.ts        # Яндекс.Диск API: рекурсивный обход папок (до 5 уровней)
└── types.d.ts
data/
└── vkr.db                    # SQLite база (создаётся автоматически при первом запуске)
```

## Ключевые архитектурные решения

### Три статуса проверки
- **pass** (Зачёт) — все автоматические пункты пройдены, нет ручных
- **fail** (Незачёт) — есть хотя бы 1 автоматически проваленный пункт
- **pending** (Ожидает проверки) — нет проваленных, но есть пункты для ручной проверки (`passed: null`)
- Преподаватель может перевести pending → pass через кнопку «Поставить зачёт» (PATCH /api/attempts)

### Чек-лист (`src/lib/checklist.ts`)
- Пункты генерируются динамически через `getChecklist(type, empMethods, compMethods, usesAI, otherMethodName?)`
- Поддерживаемые типы работ: `'project'` (магистерский проект), `'dissertation'` (магистерская диссертация)
- Каждый пункт имеет `auto: true/false` — автоматическая проверка через GPT или ручная
- Пункты длительности аудио и качества звука всегда `auto: false` (невозможно проверить программно)

### Методы исследования (`ResearchMethod`)
Доступные методы:
| Метод | Ключ | auto пунктов | Примечание |
|-------|------|-------------|-----------|
| Глубинные интервью | `interviews` | auto: true/false | Пороги: ≥10 файлов, ≥45 мин |
| Фокус-группы | `focus_groups` | auto: true/false | Пороги: ≥5 файлов, ≥45 мин |
| Анализ текстов | `text_analysis` | auto: true | Материалы + кодировочная таблица |
| Опрос | `survey` | auto: true | Выгрузка данных, метаданные, статистика |
| Количественный контент-анализ | `quant_content` | auto: true | Кодировочный лист |
| Мониторинговый анализ | `monitoring` | auto: true | Первичные/вторичные данные |
| Экспертное интервью | `expert_interview` | **auto: false** | Без порогов, всё ручная проверка |
| Другое | `other` | **auto: false** | Кастомное название метода, ручная проверка |

- При выборе «Другое» в UI появляется обязательное поле для ввода названия метода
- Кастомное название передаётся как `otherMethodName` и используется в текстах пунктов чек-листа
- Для проектов методы также доступны в секции «Анализ конкурентов» (суффикс `_comp`)

### Проверка базы данных
- Яндекс.Диск API используется для получения списка файлов по публичной ссылке
- Рекурсивный обход подпапок до 5 уровней вложенности (`fetchFolderRecursive()` в `yandex-disk.ts`)
- Файлы .xlsx/.csv парсятся (структура таблиц), .docx/.pdf — извлекается текст (превью ~1000 символов)
- Аудиофайлы — только метаданные (имя, размер)
- Описание передаётся в GPT для анализа

### Ссылка на презентацию (только проект)
- GPT проверяет наличие ссылки в документе
- Даже если ссылка найдена — результат `passed: null` (требует проверки преподавателем)
- Содержание презентации невозможно проверить автоматически

### Кнопка «Сохранить результат»
- Проверка НЕ сохраняется автоматически — студент должен нажать «Сохранить результат»
- Это позволяет перепроверять работу без траты попыток (максимум 3)

### Модуль обратной связи
- После проверки студент может оставить текстовый отзыв (необязательно)
- Отзыв сохраняется в поле `feedback` таблицы `attempts`
- Преподаватель видит отзыв в модальном окне деталей попытки

### Авторизация
- **Студент:** логин `student`, пароль `hse2025` (клиентская, без токенов)
- **Преподаватель:** логин `admin 1029`, пароль `hsevkrch12` (клиентская)
- Без авторизации формы недоступны
- Несколько студентов могут работать параллельно с одними учётными данными — конфликтов нет (каждый запрос независим)

### Валидация формы студента
- ФИО: минимум 2 слова (одно слово вызывает ошибку парсинга JSON в OpenAI API)
- Тип работы: обязательный выбор
- Файл: .docx или .pdf, до 50 МБ
- Ссылка на БД: обязательна
- Ссылка на презентацию: обязательна только для проектов
- Методы: минимум 1 (для диссертации — минимум 2)
- Если выбрано «Другое» — название метода обязательно

### Промпт GPT (`src/lib/analyzer.ts`)
- Системный промпт основан на методических рекомендациях ОП ИК НИУ ВШЭ
- «Кодировочный лист» и «Кодировочная таблица» — синонимы
- Проверка умеренно строгая: отклонение при явном отсутствии элемента или критически малом объёме
- Верификация методов: GPT проверяет, описан ли каждый заявленный метод в тексте работы; если метод заявлен, но не описан — `passed: false`
- Для диссертаций: если заявлено 2 метода, но текст описывает только 1 — `mixed_method: passed: false`
- Поддержка моделей: gpt-4o-mini, gpt-5, gpt-5-nano, o3-mini и др. Автоматическое определение параметров (temperature, max_tokens vs max_completion_tokens)

### Панель преподавателя (`src/app/teacher/page.tsx`)
- Сводная таблица всех студентов: ФИО, тип работы, попытки, статусы
- 5 карточек статистики: всего, зачёт, незачёт, ожидает проверки, не сдали
- Кнопка «Поставить зачёт» для попыток со статусом pending или fail (для fail требуется подтверждение)
- Кнопка «Поставить незачёт» для принудительного снятия зачёта
- Модальное окно с детальным чек-листом, комментарием студента, информацией о документе
- Два поля комментариев в модалке: «Отзыв преподавателя на работу» (виден студенту в итоговом отчёте) и «Технический комментарий» (виден только преподавателю)
- Если файл работы утерян — показывается «⚠️ Файл утерян — попросите студента загрузить работу заново»
- Секция «Итоговый отчёт»: toggle «Создать отчёт» / «Закрыть отчёт», статус (Опубликован/Закрыт)
- Экспорт сводной таблицы в Excel (xlsx) и PDF (window.print)
- Скачивание загруженных работ через `/api/download?id=N`

### Итоговый отчёт / Статус работы (`src/app/report/page.tsx`)
- Вкладка называется «Итоговый отчет/Статус работы» в шапке всех страниц
- Студент вводит ФИО → **всегда** видит карточку статуса загрузки (загружена ли работа и когда)
- Полные результаты (Зачёт/Незачёт/Ожидает + чек-лист) доступны только после публикации отчёта
- Без авторизации — идентификация только по ФИО
- Поле `tech_comment` не передаётся студенту — только `teacher_review`
- Поиск по ФИО устойчив к trailing spaces: `TRIM(student_name) = TRIM(?)`

### Снапшот при публикации отчёта
- `POST /api/report` сохраняет IDs попыток всех студентов **на момент нажатия** «Создать отчёт» в `report_snapshot_ids`
- Новые работы, поданные после публикации, **не появляются** в отчёте до следующего нажатия «Создать отчёт»
- `GET /api/report` ищет студента только в снапшоте (не во всей таблице `attempts`)
- `DELETE /api/report` очищает и флаг, и снапшот

### Защита эндпоинта отчёта (`src/app/api/report/route.ts`)
- `POST /api/report` требует `{ password: '1234' }` в теле — без пароля возвращает 401
- `DELETE /api/report` закрывает отчёт (очищает флаг и снапшот) — тоже требует пароль
- Пароль хранится в константе `REPORT_PASSWORD` в route.ts

### Кэширование авторизации (`src/lib/authCache.ts`)
- `saveAuth(role)` / `readAuth(role)` / `clearAuth(role)` — localStorage с TTL 24 часа
- Ключи: `vkr-auth-student`, `vkr-auth-teacher`
- При входе сохраняется timestamp; при загрузке страницы проверяется TTL
- Кнопка «Выйти» очищает кэш и сбрасывает состояние

### Схема БД (`src/lib/db.ts`)
Таблица `attempts`:
| Поле | Тип | Описание |
|------|-----|----------|
| id | INTEGER PK | Автоинкремент |
| student_name | TEXT | ФИО студента |
| work_type | TEXT | 'project' или 'dissertation' |
| attempt_number | INTEGER | 1–3 |
| status | TEXT | 'pass', 'fail', или 'pending' |
| results_json | TEXT | JSON массив CheckResult[] |
| extracted_text_preview | TEXT | Первые ~2000 символов текста |
| file_name | TEXT | Имя загруженного файла |
| file_path | TEXT | Путь к загруженному файлу на диске |
| db_link | TEXT | Ссылка на Яндекс.Диск |
| pres_link | TEXT | Ссылка на презентацию |
| methods_json | TEXT | JSON: {emp, comp, otherMethodName} |
| uses_ai | INTEGER | 0/1 — использует ли ИИ |
| feedback | TEXT | Комментарий студента для преподавателя |
| teacher_review | TEXT | Отзыв преподавателя на работу (виден студенту в отчёте) |
| tech_comment | TEXT | Технический комментарий преподавателя (приватный) |
| created_at | DATETIME | Дата создания |

Таблица `settings`:
| Поле | Тип | Описание |
|------|-----|----------|
| key | TEXT PK | Ключ настройки |
| value | TEXT | Значение |

Ключи settings: `digest_email`, `current_wave`, `report_generated_at`, `report_snapshot_ids` (JSON-массив ID попыток, включённых в снапшот)

Миграции выполняются автоматически при инициализации БД в `initDb()`.

## Переменные окружения (.env)
```
OPENAI_API_KEY=sk-...          # Обязательно
OPENAI_MODEL=gpt-4o-mini       # Текущая модель (можно: gpt-4o-mini, gpt-5-nano, gpt-5, o3-mini)
SMTP_HOST=smtp.gmail.com       # Для email-дайджеста (опционально)
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
DIGEST_RECIPIENT=professor@hse.ru
```

## Команды
```bash
npm install          # Установка зависимостей
npm run dev          # Запуск в режиме разработки (localhost:3000)
npm run build        # Сборка production
npm start            # Запуск production-сборки
```

## Локальный запуск (с нуля)
```bash
# 1. Клонировать репозиторий
git clone https://github.com/leonovvladimirhq-svg/vkr-checker-hse.git
cd vkr-checker-hse
git checkout vkr-version-1

# 2. Установить зависимости
npm install

# 3. Создать файл .env в корне проекта
# Скопировать содержимое из раздела "Переменные окружения" выше
# Минимально нужен только OPENAI_API_KEY

# 4. Запустить в режиме разработки
npm run dev
# Сервис будет доступен на http://localhost:3000

# Примечание: папка data/ и файл vkr.db создаются автоматически при первом запуске
```

## Деплой (сервер)
- **Сервер:** Ubuntu 24.04, 1 CPU, 1 ГБ RAM + 1 ГБ Swap (`/swapfile`)
- **Домен:** вкр-чекер.рф (Punycode: `xn----ctbkawc3be3d.xn--p1ai`)
- **IP:** 89.125.107.91
- **Nginx:** reverse proxy на localhost:3000, SSL через Certbot (Let's Encrypt)
- **Nginx конфиг:** `/etc/nginx/sites-available/vkr-checker`
  - `client_max_body_size 50m` — для загрузки файлов
  - `proxy_read_timeout 180` — для долгих GPT-запросов
- **Процесс-менеджер:** PM2 (`pm2 start ecosystem.config.js`), лимит памяти Node.js: `--max-old-space-size=768`
- **Автозапуск:** настроен через `pm2 startup`
- **Папка:** `/root/vkr-checker-hse/`
- **БД на сервере:** `/root/vkr-checker-hse/data/vkr.db`

### Обновление на сервере
```bash
ssh root@89.125.107.91
cd /root/vkr-checker-hse
git pull
npm install
npm run build
pm2 restart vkr-checker
```

## Модуль курсовой работы (`/course`)

Параллельный контур самопроверки **курсовых работ** студентов ОП ИК. Принципиально отличается от ВКР-модуля: акцент не на pass/fail, а на **интеллектуальных рекомендациях** по улучшению.

### Архитектура модуля
- **Страница студента:** `src/app/course/page.tsx` — авторизация (`student` / `hse2025`, та же как у ВКР), форма (ФИО, **тема работы** — обязательное поле ≥5 символов, тип КР, файл), результат (карточка «Готовность к зачёту» + приоритетные рекомендации без лимита + разбор разделов), кнопка **«Отправить работу преподавателю»**.
- **API анализа:** `src/app/api/course/check/route.ts` — `POST /api/course/check`. Парсит файл → вызывает GPT → автоматически пишет попытку в `course_attempts` (каждая загрузка логируется).
- **API отправки преподавателю:** `src/app/api/course/submit/route.ts` — `POST`. Сохраняет файл в `data/course-uploads/<attemptId>.<ext>`, проставляет `submitted_at`, `file_path`, `work_title`.
- **API преподавателя:** `src/app/api/course/teacher/route.ts` (GET list/detail), `download/route.ts` (GET оригинал), `reanalyze/route.ts` (POST: повторный GPT-анализ + генерация Word-отзыва).
- **Анализатор:** `src/lib/course-analyzer.ts` — промпт, типы, вызов OpenAI, нормализация ответа. Формат вывода `CourseAnalysisResult` (см. ниже). Тон промпта — конструктивный, без категоричных формулировок; роль — «опытный научный руководитель», без слов «виртуальный помощник» / «бот» / «ассистент» в текстах.
- **Word-генератор:** `src/lib/course-review-doc.ts` (рендер .docx через `docx`-библиотеку, структура из шаблонов «Шаблон отзыва - ИКР/КП 2026.docx» = Приложение 20.1/20.2 Программы практики) + `src/lib/course-review-prompt.ts` (GPT заполняет поля шаблона).
- **Knowledge base:** `src/lib/methodology/` — каталог `.md`-файлов с методическими материалами. На вход GPT кладётся через `getMethodologyForCourse(type)`, который конкатенирует:
  - `coursework-2024.md` — **актуальная** методичка для 2024–2025 г. набора (структура ИКР/КП, критерии для каждого структурного элемента, стиль и язык, этика). Главное отличие от методички 22/23: ИКР теперь требует полноценную эмпирическую главу (не только дизайн).
  - `programme-2025.md` — Программа практики 2025 г. набора. Регламент: жёсткие пороги объёма (≥90 000 знаков для рус, ≥85 000 для англ), критерии допуска к защите, правила использования ИИ (что можно/нельзя, обязательность раздела «Описание применения генеративной модели»), требования к базам данных по типам исследований (Приложение 19), ГОСТ-стандарты оформления, шкала оценивания с весами (ИКР: введение 0.05 + концепт 0.25 + методы 0.25 + анализ 0.25 + заключение 0.20).
  - `coursework.md` — LEGACY-методичка 22/23 г. набора. По умолчанию НЕ используется; остаётся как fallback на случай отсутствия новых файлов.

  При добавлении новых документов (Гайдбук, программы практики бакалавриата и т.д.) — кладите `.md` в `src/lib/methodology/` и подключайте в `index.ts`.
- **БД:** `src/lib/db-course.ts` + таблица `course_attempts` (отдельная от `attempts`, схема инициализируется лениво в `ensureSchema()`).

### Типы курсовых
- `research` — Исследовательская курсовая (перерастает в МД)
- `project` — Курсовой проект (перерастает в МП)

Ожидаемые разделы определяются константой `EXPECTED_SECTIONS` в `course-analyzer.ts`.

### Формат вывода (`CourseAnalysisResult`)
- `overallSummary` — 2–3 предложения общей оценки
- `readinessStatus` + `readinessStatusText` — общая оценка готовности к зачёту, одно из 5: `ready_for_credit` / `almost_there` / `right_direction` / `fix_then_defend` / `critical_issues`
- `errorClassification` — счётчики по 5 категориям (structural, content, formatting, ai_usage, other)
- `structuralAnalysis` — ожидаемые/отсутствующие/лишние разделы + поразделный разбор (`section`, `present`, `wordCount`, `verdict: ok|too_short|too_long|missing`, `comment`)
- `logicAndCoherence` — `issues` / `strengths`
- `textQuality` — `issues` / `strengths`
- `recommendations` — расширенный список с категориями (`structure | content | logic | text_quality | formatting`)
- `priorityAdvice` — массив приоритетных советов **без верхнего лимита** (мин. 3, типично 5–15). Не обрезается, не дополняется плейсхолдерами.
- `disclaimer` — фикс. текст «Итоговое решение по работе принимает научный руководитель»

### Схема `course_attempts`
| Поле | Тип | Описание |
|------|-----|----------|
| id | INTEGER PK | |
| student_name | TEXT | Trimmed ФИО |
| course_type | TEXT | 'research' \| 'project' |
| attempt_number | INTEGER | без лимита, инкрементируется per студент |
| file_name | TEXT | имя загруженного файла |
| word_count, page_estimate | INTEGER | метаданные документа |
| headings_json | TEXT | JSON массив найденных заголовков |
| extracted_text_preview | TEXT | первые ~2000 символов |
| analysis_json | TEXT | сериализованный `CourseAnalysisResult` |
| top5_json | TEXT | колонка-история: хранит сериализованный `priorityAdvice` |
| work_title | TEXT | тема работы (обязательное поле формы, ≥5 симв.) |
| file_path | TEXT | путь к сохранённому файлу в `data/course-uploads/<id>.<ext>` (только после Submit) |
| submitted_at | DATETIME | момент нажатия «Отправить преподавателю»; NULL = только проанализирована |
| readiness_status | TEXT | дубль из analysis_json для быстрых SELECT в таблицу преподавателя |
| readiness_text | TEXT | дубль текста статуса |
| created_at | DATETIME | |

**Файл курсовой сохраняется на диск только при нажатии «Отправить преподавателю»** (а не при анализе). Каждая отправка создаёт отдельную запись, файл лежит в `data/course-uploads/<attemptId>.<ext>`.

### Принципы UX
- Авторизация через тот же localStorage-ключ `vkr-auth-student` (без отдельного экрана входа для курсовой)
- Дисклеймер «не выполняем работу за студента» виден на форме и в результате
- Без лимита попыток — каждая загрузка логируется как попытка (учёт использования инструмента, см. протокол встречи 30.04.26)
- Шапка с подсветкой активной вкладки «Курсовая работа», ссылка добавлена во все 4 страницы
- **Тон ответа** — конструктивный/поддерживающий, без категоричных и демотивирующих формулировок (правила прописаны в системном промпте)

### Преподавательская секция «Курсовые работы студентов» (`/teacher`)
Расположена в `src/app/teacher/page.tsx` отдельно ниже сводной таблицы ВКР (таблица ВКР не модифицирована). Колонки:
- № / ФИО / Тема работы / Тип (ИКР/КП) / Дата отправки / Итог анализа (бейдж readiness) / Действия.
- **Действия**:
  - 👁 Подробнее — модалка с overallSummary, priorityAdvice, sections, recommendations.
  - 📥 Скачать — оригинальный .docx/.pdf студента (через `/api/course/download`).
  - 🔄 Повторное исследование — повторный GPT-анализ (`/api/course/reanalyze`); генерирует Word-отзыв по шаблону ИКР (Приложение 20.1) или КП (Приложение 20.2) и **автоматически скачивает** преподавателю (через blob URL).

Шаблоны отзывов соответствуют присланным `Шаблон отзыва - Исследовательская курсовая работа 2026.docx` и `Шаблон отзыва - Курсовой проект 2026.docx`; программная реализация в `src/lib/course-review-doc.ts` (структура таблиц БД / ИИ / Характеристика работы с весами критериев + подпись руководителя).

## История изменений (последний коммит сверху)

### 2026-04-24: Snapshot-отчёт + статус загрузки работы
Файлы: `report/route.ts`, `report/page.tsx`, `db.ts`, `page.tsx`, `teacher/page.tsx`
1. **Снапшот при публикации** — `POST /api/report` сохраняет IDs попыток на момент нажатия «Создать отчёт» в `report_snapshot_ids`; новые работы, поданные позже, не попадают в отчёт.
2. **Статус загрузки** — `GET /api/report` теперь **всегда** возвращает `submissionStatus` (загружена ли работа и когда), независимо от публикации отчёта.
3. **Новый UI страницы /report** — карточка «Работа загружена» / «Работа не найдена» показывается сразу; полный чек-лист только после публикации; отдельные состояния для «работа не в снапшоте» и «работа не найдена».
4. **Переименование вкладки** — «Итоговый отчет» → «Итоговый отчет/Статус работы» на всех 3 страницах.
5. **Новые DB-функции** — `getStudentSubmissionStatus()`, `getAttemptByIdIfInSnapshot()`.

### 2026-04-22: 6 улучшений — безопасность, UX, стабильность
Файлы: `report/route.ts`, `teacher/page.tsx`, `page.tsx`, `db.ts`, `attempts/route.ts`, `check/route.ts`; сервер: `ecosystem.config.js`, `/swapfile`
1. **Авторизация «Создать отчёт»** — `POST /api/report` требует пароль `1234`; случайный вызов URL не публикует отчёт.
2. **Кнопка «Закрыть отчёт»** — `DELETE /api/report` + toggle в панели преподавателя: видно текущий статус (Опубликован/Закрыт).
3. **PM2 лимит памяти** — `--max-old-space-size=768` через `ecosystem.config.js`; предотвращает OOM-краши на 1 ГБ сервере.
4. **Своп 1 ГБ** — `/swapfile` на сервере, добавлен в `/etc/fstab`; буфер при пиковой нагрузке.
5. **Оверлей при сохранении** — при нажатии «Сохранить результат» появляется экран «Не закрывайте страницу!»; предотвращает потерю попытки.
6. **Заметный дисклеймер** — предупреждение о предварительном результате оформлено amber-блоком с иконкой ⚠️.
7. **Friendly-ошибка скачивания** — если файл работы утерян, преподаватель видит «Файл утерян» вместо тихой 404.
8. **Trim имён студентов** — `TRIM()` в SQL-запросах + `trim()` при сохранении; устраняет дубли из-за лишних пробелов в ФИО.

### 2025-04-12: Итоговый отчёт, комментарии преподавателя, кэш авторизации
Файлы: `db.ts`, `attempts/route.ts`, `report/route.ts` (новый), `report/page.tsx` (новый), `teacher/page.tsx`, `page.tsx`, `authCache.ts` (новый)
1. **Итоговый отчёт** — новый модуль `/report`: преподаватель нажимает «Создать отчёт» → студенты могут посмотреть результаты по ФИО. Архитектура: флаг в settings + live-данные из attempts.
2. **Отзыв преподавателя** — поле `teacher_review` в модалке преподавателя, виден студенту в итоговом отчёте.
3. **Технический комментарий** — поле `tech_comment` в модалке преподавателя, приватный (не передаётся студенту).
4. **Кэш авторизации** — localStorage с TTL 24 часа для студента и преподавателя + кнопка «Выйти».
5. **Принудительный зачёт** — кнопка «Поставить зачёт» теперь доступна и для статуса «Незачёт» (с подтверждением).
6. **Переименование** — «Отзыв (необязательно)» → «Комментарий для преподавателя».
7. **Согласие на ПД** — текст о согласии на обработку персональных данных на всех страницах.
8. **Навигация** — вкладка «Итоговый отчет» добавлена в шапку всех страниц.

### 2025-04-08: 5 исправлений по обратной связи тестировщиков (Елена и Саша)
Файлы: `checklist.ts`, `page.tsx`, `check/route.ts`, `analyzer.ts`, `db.ts`, `attempts/route.ts`, `teacher/page.tsx`
1. **Новые методы** — добавлены «Экспертное интервью» и «Другое» (с кастомным названием) в обе секции (эмпирика + конкуренты). Все пункты `auto: false` — ручная проверка без пороговых значений.
2. **Переименование** — «Методы анализа текстов» → «Другие методы анализа текста» в секции конкурентов.
3. **Строгость GPT** — добавлено правило верификации: GPT проверяет, описан ли каждый заявленный метод в тексте. Если нет — `passed: false`.
4. **Валидация ФИО** — минимум 2 слова (одно слово вызывало ошибку JSON.parse в OpenAI API).
5. **Модуль обратной связи** — студент может оставить отзыв; преподаватель видит его в панели.

### 2025-04-07: 3 исправления по обратной связи тестировщика
Файлы: `yandex-disk.ts`, `analyzer.ts`, `db.ts`, `attempts/route.ts`, `check/route.ts`, `page.tsx`, `teacher/page.tsx`, `students/route.ts`
1. **Рекурсивный обход Яндекс.Диска** — до 5 уровней вложенности с пагинацией.
2. **Презентация → ручная проверка** — `passed: null` даже если ссылка найдена.
3. **Третий статус pending** — «Ожидает проверки преподавателем» + кнопка «Поставить зачёт».

### 2025-04-07: Авторизация + смена модели
1. Клиентская авторизация на странице студента.
2. Переключение модели GPT (gpt-5-nano → gpt-4o-mini).

## Контакты
- **Разработчик:** vleonov@hse.ru
- **Обратная связь по результатам:** example@hse.ru

## GitHub
- **Репозиторий:** https://github.com/leonovvladimirhq-svg/vkr-checker-hse
- **Основная ветка разработки:** `vkr-version-1`
